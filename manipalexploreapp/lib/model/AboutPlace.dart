class AboutPlace {
  String category, image, name, distance;
  double initialRating;
  Function onTapPlaceCard;

  AboutPlace(
      {this.category,
        this.image,
      this.name,
      this.distance,
      this.initialRating,
      this.onTapPlaceCard});
}
