class AboutPlace {
  String image, name, distance;
  double initialRating;
  Function onTapPlaceCard;

  AboutPlace(
      {this.image,
      this.name,
      this.distance,
      this.initialRating,
      this.onTapPlaceCard});
}
